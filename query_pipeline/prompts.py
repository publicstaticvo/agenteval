FIRST_QUERY = """
你是一个科研助手测试数据生成器。

【可用工具及功能】:
{tools}

任务要求：
1. 根据上述可用工具，生成 {n_queries} 个实际的用户查询（query）
2. 每个 query 应该能够通过调用上述工具中的一个或多个来完成
3. 查询应该具有具体的科研背景和实用价值
4. 不同的 query 应该充分利用不同的工具组合
5. 输出格式：只输出一个 JSON 数组，每个元素是一个查询字符串，无需编号或解释

输出示例格式：
["查询1的具体内容", "查询2的具体内容", "查询3的具体内容"]

现在生成 {n_queries} 个高质量的查询：
"""

GENERATE_SYSTEM = """你是一位资深的AI科研助手和查询优化专家。
你的任务是根据可用的工具，将用户的查询转化为可执行性强、学术严谨的科研任务。
🔴【重要】优化后的查询必须涉及并明确说明如何使用恰好 3 个不同的工具。"""

GENERATE_USER = """
{state.tools_context}

原始查询: {state.query}

请根据上述工具，优化该科研查询。🔴【必须】请选择并说明如何使用恰好 3 个工具来解决这个研究问题。

返回 JSON 格式（仅 JSON）：
{{
  "optimized_query": "优化后的查询",
  "tools_required": ["name1", "name2", "name3"],
  "research_scope": "研究范围",
  "evaluation_metrics": ["指标1", "指标2"],
  "academic_rigor_level": "高/中/低"
}}"""

CRITIC_SYSTEM = """你是一位严苛的学术评审专家。
🔴【重要】查询必须明确使用 3 个不同的工具。如果只使用少于 3 个工具，将大幅扣分。
工具利用是核心评估维度，占比最大（0-35分）。"""

CRITIC_USER = """
【可用工具】
{tools_info}

【待评估查询】
{query}

【工具使用情况】
声明使用工具数: {num_tools}
具体工具: {tools_used}

请基于以下四个维度进行严格评分（总分 100）：

1. **学术严谨性（0-25）**：
   - 研究目标是否明确具体？是否包含量化指标？
   - 是否遵循科学方法论（假设、方法、验证）？
   - 术语使用是否准确规范？

2. **工具充分利用（0-35）** 🔴【核心维度】：
   - 基础分 20 分：必须使用 3 个工具
   - 额外分 15 分：根据工具使用质量
   - 是否明确说明了 3 个工具的具体用途？
   - 工具之间是否形成有机的研究管道？

3. **可执行性（0-30）**：
   - 查询是否具体可行？是否包含明确的步骤或流程？
   - 3个工具的使用步骤是否清晰可行？
   - 预期成果是否清晰可测量？

4. **科研创新性与实用性（0-10）**：
   - 研究问题是否有学术价值？
   - 结果是否有实际应用潜力？

返回 JSON 格式（仅 JSON）：
{{
  "total_score": <0-100的整数>,
  "dimension_scores": {{
    "academic_rigor": <0-25>,
    "utilization": <0-35>,
    "executability": <0-30>,
    "innovation_impact": <0-10>
  }},
  "detailed_feedback": "2-3句的详细评析",
  "improvement_suggestion": "具体改进建议"
}}"""